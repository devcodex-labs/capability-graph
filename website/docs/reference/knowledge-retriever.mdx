---
title: KnowledgeRetriever 接口
description: KnowledgeRetriever targets、访问能力、命中、索引证据和零结果合同参考。
---

# KnowledgeRetriever 接口

<span className="cg-status">仅合同</span>

`retrieve(input, access)` 只处理 Core 展开的最终 targets。每个 target 含能力身份、knowledgeId、声明 locator 和经过的 Collection IDs，不含绝对知识根。

命中必须提供能力身份、knowledgeId、contentId、source、UTF-8 `startOffset`/`endOffset`、snippet 和可选 score。

索引证据必须精确覆盖最终 targets Provider，证明每个文档的 source/indexed contentId、mapping/config revision、观察时间和 freshness。即使没有命中，也必须返回可验证证据和明确 `knowledgeState`。

Core 会限制 hits 数量、snippet 字节、内容边界和访问范围。过期证据不会降级为成功空结果。

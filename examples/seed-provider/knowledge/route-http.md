# HTTP 路由

HTTP 路由将方法和路径绑定到宿主应用的处理器。`seed.http::route.http` 是这一能力类型，不是某个项目的具体路由。

## 定位已有路由

1. 指定项目与环境，通过已配置的 Runtime Adapter 查询 `route.http` 实例。
2. 检查返回的方法、路径、来源身份和观察依据。相同实例名在其他环境中不是同一事实。
3. 来源不可达时修复采集，不能把失败解释为路由不存在。旧构建与当前源码可能不一致，需要重新构建或重新观察。

Core 不发送业务请求，也不自动启动服务。真实参考程序由使用者或测试显式启动。
